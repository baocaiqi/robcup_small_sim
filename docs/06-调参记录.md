HEAD


### 第 20 轮：门将上角封堵三修（2026-09-03，feat/goalie-upper 分支）
- **真机复盘驱动**（9/2 下午 + 9/3 早场 vs demo，连续 5+ 丢球全部打门柱内侧上角
  y∈[100,105]，含开场 3.2s 老套路）：
  A 类：门将 y≈83~88 偏低（球进 y≈102~105）——轨迹外推不足；
  B 类：门将 y 已到位（99.8~102.2）仍漏——球越过站位线后外推 t>1 把拦截点推到球后。
- **改动（roles.cpp run_goalie，UTF-16 安全编辑保持编码）**：
  1. `out_x`（站位线）→ 球距己方门线 <15cm 时贴门 3cm（原来只用动态 depth≥10，
     gx 贴门逻辑成了死代码；球在站位线与门线之间时门将站 10cm 外追不及）；
  2. 兜底外推 `t = (out_x-gx)/(bx-gx)` → clamp [0,1]（球已越过站位线时不再把拦截点
     外推过球，防拦截点跑到球后方）；
  3. 封堵上角：`on_target && |y_at_goal-90| > |iy-90|` 时 `iy = y_at_goal`（预测进门点
     比站位线轨迹点更靠上/下角 → 直接守进门点）。
- **效果**：待真机 A/B（目标：上角类丢球消失、门区违规不增）；offline_test 17 项全绿。
- 备注：demo 开场 3.2s 控停冲射套路不在本次范围（属 GK 冲球/开球另案）。
### 第 20 轮补充：门前冲球分支加封堵（9/3 午间部署二修）
- **二修背景**：首修部署后 3 场真机 1 胜 2 负（1:0 胜 / 0:1 / 1:3）；4 个丢球：
  上角 y≈103/104 两球（门将 y≈88 偏低）+ 下角 y≈74 两球（门将冲球出）——
  首修只覆盖"站位拦截"分支，而 demo 进球全走"门前抢球/冲球"分支
  （球距门<45 且对方持球<25 → 门将冲球推穿目标在球身上 → demo 变向换侧进门）。
- **改动（roles.cpp run_goalie）**：冲球分支拆两路——
  ① `ball_still`（球近静止）→ 保留原冲球推穿（清门球，防门球死循环）；
  ② 对方持球压门（<45cm 且对方离球<25cm）→ **不再冲球**，改为封球-门连线：
     深度 = min(40, max(kGuardDist, 球距门-12))，y = 球心连线在球前 12cm 处值，
     clamp [78,102] + 罚球区——对方碰球期间门将不退门线（保第15轮教训）。
- **效果**：待下一场真机确认（目标：上/下角贴门推射类失球消失；门球死循环不回潮）。
### ⚠️ 工具勘误：rlg_analyzer 进球判定不可靠（2026-09-03）
- **现象**：rlg 报 9/3 12:28 场"黄 3:1"，官方 SimuroSot5.log 实为 **1:1**；
  rlg 报 11:42 场"黄 1:0"，官方实为 **0:0**；9/3 8:32 场 rlg"黄 2:0"，官方 1:0。
- **根因**：rlg_analyzer 用 `ball.x > 220（或 <0）且 y∈门宽` 判进球；真机 rlg 坐标
  含 ~2cm 偏移，球擦门线/反弹（x≈221~223）被误报为进球，平台未判。
- **铁律（复盘必读）**：比分以 **C:\Strategy\SimuroSot5.log 的 Place Kick 事件**
  为准（Place Kick X = X 队开球 = 对方刚进球，比分 (黄:蓝)）；rlg 只用于看站位/轨迹。
- **部署后 4 场真机真实战绩（2026-09-03）**：11:26 1:0 胜 / 11:42 0:0 平 /
  12:03 1:2 负 / 12:28 1:1 平 → **1 胜 2 平 1 负，场均失 0.75**（vs 9/2 下午 6 负 4 平
  场均失 ~1.6+）——门将上角三修+冲球拆路**真实有效**，但 12:03 仍 1:2 负，
  擦线/贴门推射仍有漏，继续观察。
- **待办**：rlg_analyzer 加擦线容忍（阈值 222 / 连续帧确认）或输出权威比分提示。
- **2026-09-05（队员C：P0 确定性升级：D1 防守纪律线 + D2 pass 射门收益）**
  - **D1-0.5a 禁推区避让**：field_info.hpp 新增 `in_forbidden_push_area` / `clamp_out_forbidden_push_area`（四角 8cm 可配置，`CFG_CORNER_PUSH_SIZE`，规则 7.2.6）。位置按「四角矩形」实现，**坐标口径待 D6 真平台校准**（规则图 4 显示黄色区在场地两端 8cm 标注，docs/00 实测记录为四角）。
  - **D1-0.5b 门区/禁区纪律（rules 7.10.1/7.10.3/7.10.4/7.16）**：
    - ACTIVE 进对方门区计时（world_model.hpp 新增 `active_goal_area_frames[5]`），≥18 帧强制撤离到门区前缘外 5cm（规则 20 帧，留 2 帧余量）。
    - 对方禁区己方非门将 ≥2 时（`kActiveOppPenaltyHeadroom=2`），ACTIVE 跳过射门、优先传球、无传球带出禁区（防 3 人停留>20 帧判点球 / 禁区 4 人判对方门球）。
  - **D1-0.5c 僵局规避（rule 7.14）**：球速 <0.8cm/帧（`kDeadBallSpeed`）且球在禁推区 → 主动带向场中（防禁推区面对面推球 50 帧判争球）。
  - **D2 pass 三步升级**（pass.cpp）：
    - ① 射门收益：`receive_point_can_shoot` 拷贝 wm、设假球位调 plan_shoot 预判「接到能否射门」，能射门接应点评分 −60（`SHOOT_BONUS`）。
    - ② 到达时间：`opp_arrive_time`（用 opp_vx/opp_vy，静止按 1.0 兜底）vs 队友到达时间（2.0cm/帧），对手先到 +40（`ARRIVE_PENALTY`）。
    - ③ 速度威胁：`speed_threat` 对威胁半径内朝接应点逼近的对手按接近速度加权（权重 10，`SPEED_THREAT_W`）。
  - **验证**：offline_test 16/16 全 PASS（原 11 项未破 + 新增 corner_push_avoid / active_goal_area_limit / penalty_area_headcount / pass_shoot_bonus / pass_arrive_speed）。
  - **待办**：禁推区坐标 D6 真平台校准；sim_bench 新旧自博弈回归；数值调参（SHOOT_BONUS/ARRIVE_PENALTY/SPEED_THREAT_W/CFG_CORNER_PUSH_SIZE）。

## 2026-09-05 D7 缓冲调参：禁区拥挤阈值 2→3

- **改动**：roles.cpp `kActiveOppPenaltyHeadroom` 2→3（规则允许对方禁区 2 人任意停留；3 人>20帧/4 人任意时长才判点球，原 2 人即带出过度保守）
- **测试**：offline_test 拥挤场景 2 人→3 人（home[3] 移入禁区 (35,100)+opp 挡线），16/16 全绿
- **sim_bench 50 场对比（scripted seed=1）**：进球 6.6→6.8、失球 1.9 持平、净胜 +4.7→+4.9、控球 34.1→34.7%、门区2+人违规 8.0→8.2 次/场（仍远低于基线 12.5）、胜负 47/1/2→48/1/1
- **结论**：找回 0.2 失球代价的进攻，判罚风险基本不增；存档 sim_eval_p0_d7.json

## 2026-09-05 D7 微调 B（已回退）：门区撤离目标 y 跟球

- **尝试**：roles.cpp 门区 18 帧撤离点 y 基准从 `r.y`（自身）改为 `wm.ball.y`（球），意图缩短回抢路径
- **结果（sim_bench 50 场 scripted seed=1）**：进球 6.8→6.6、净胜 +4.9→+4.7、射门 1008.9→989.7，变差
- **原因**：撤到球正外侧偏离自身原抢射有利位，回抢反而变慢
- **结论**：已回退为 y 跟自身（注释保留“曾试跟球变差”），恢复最优 6.8:1.9；D7 最终有效改动仅 `kActiveOppPenaltyHeadroom` 2→3
bbeb1d5 (feat(P0+D7): 防守纪律状态机 + 多因子传球评分 + 禁区阈值调优)
