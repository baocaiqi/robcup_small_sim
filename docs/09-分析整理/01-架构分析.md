# 架构分析（cbm 知识图谱导出）

> 来源：codebase-memory 索引 `robcup_small_sim`（556 节点 / 1185 边，commit 1f0115c）
> 索引工具结果，未人工修饰。与 ua 图谱（`knowledge-graph.json`）互补：cbm 偏调用图/热力，ua 偏语义分层。

## 概览

- 语言：C++（14 文件）+ Python（4 文件）
- 节点：Section 174 / Function 87 / File 58 / Module 58 / Field 55 / Variable 48 / Folder 20 / Method 19 / Class 16 / Macro 16 / Enum 3
- 边：DEFINES 512 / USAGE 308 / CALLS 143 / WRITES 87 / IMPORTS 30 / SEMANTICALLY_RELATED 12 / FILE_CHANGES_WITH 8 / DEFINES_METHOD 7

## 分层（依赖方向）

| 层 | 判定 | 说明 |
|----|------|------|
| **simuro5** (include 内核) | core，fan-in 22 | 被所有模块依赖，只出接口不调用业务——架构底座 |
| **formation** | core，fan-in 6 | 摆位核心，被 dll 导出壳与死球流程调用 |
| **defense** | entry | 防守模块，只出调用（调 simuro5 几何/场地） |
| **situation** | entry | 局势分析，只出调用 |
| **dll_blue / dll_yellow** | entry | 平台入口壳，只出调用 |
| **py** (tools) | entry | Python 工具，独立入口 |
| len/print/range/dict/list | leaf/core | Python 内建 |

## 模块边界（CALLS 跨包）

```
defense → simuro5    11 次   防守逻辑大量依赖内核工具
situation → simuro5   9 次   局势分析依赖内核
dll_yellow → formation 3     导出壳调摆位
dll_blue → formation   3
formation → simuro5    2
```

## 热力点（fan-in 最高）

| 符号 | 被引用 | 含义 |
|------|--------|------|
| `TeamContext.our_goal_x` | 14 | 己方球门 x——全队镜像参数核心 |
| `TeamContext.attack_dir` | 13 | 进攻方向——蓝黄镜像开关 |
| `in_penalty_area` | 5 | 禁区判定 |
| `put` / `field_rot` / `goalie_rot` / `M` | 各 5 | 摆位核心工具 |
| `TeamContext.opp_goal_x` | 4 | 对方球门 x |

结论：`TeamContext`（蓝黄镜像参数化）是全库最热的耦合点，改动需全量回归。

## 社区聚类（de-facto 模块）

| 簇 | 成员 | 内聚 | 内容 |
|----|------|------|------|
| 1 | include | 0.82 | 世界模型/防守/站位核心（our_goal_x、plan_defense、update_stand_points） |
| 17 | tools | 1.0 | Python 复盘工具 |
| 11 | src | 1.0 | formation 摆位族 |
| 12 | src | 0.71 | strategy 调度族（run、threat_from_state、RunStrategy） |
| 3 | include+src | 0.8 | 盯人防守族（pick_mark_target、mark_threat） |
| 14 | include | 1.0 | 角度工具族（normalize_angle/angle_diff/angle_to） |

## 入口点

- `src/dll_blue.cpp` RunStrategy / DllMain / SetBall / SetFormerRobots / SetLaterRobots / SetBlueTeamName
- `src/dll_yellow.cpp` 同构镜像
- `tools/py/rlg_analyzer.py` main（复盘）、`tools/py/mark_analysis.py` main（跑位分析）

## 覆盖注意

索引 4 个文件部分解析（parse_partial），图内函数可能不全，分析这些文件时以源码为准：
- `include/simuro5/simuro_interface.hpp`（L91-103：官方接口区）
- `src/offline_test.cpp`（全文件：测试代码）
- `src/pass.cpp`（多处）
- `src/roles.cpp`（全文件：角色行为，实际含 spread_y/run_goalie/run_active/run_passive/run_assist/run_midfield 6 函数）

详见 `06-cbm索引报告.md`。
