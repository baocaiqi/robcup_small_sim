# 03 · 开发进度跟踪

> 维护人：全队（每次重要节点更新）；用途：跨会话衔接

## 里程碑

| 轮次 | 目标 | 状态 |
|------|------|------|
| 第 0 轮 | 代码库搭建：接口/构建/骨架/文档 | ✅ 完成 |
| 第 1 轮 | 平台能加载 DLL、能追球、能进一个球 | ⬜ |
| 第 2 轮 | 摆位 12 态对齐规则、防守不送点球 | ⬜ |
| 第 3 轮 | 传球/跑位/攻防平衡，能打完整 5 分钟 | ⬜ |
| 第 4 轮 | 调参 + 日志复盘迭代，提交资格材料 | ⬜ |

## 第 0 轮已完成清单（2025-08-02）

- [x] 官方平台解压分析（接口/坐标系/位数/导出符号）→ `docs/02`
- [x] 规则 PDF 提炼 → `docs/00`
- [x] 官方平台与规则调研 → docs/00、docs/02
- [x] CMake 工程（MSVC 32 位）编译通过，导出符号与官方一致
- [x] 核心骨架：geometry/field_info/world_model/situation/role_assignment/motion/shoot/pass/defense/roles/formation/strategy
- [x] offline_test 全绿（300 帧策略 + 12 态摆位）
- [x] Python：constants/geometry/rlg_analyzer（.rlg 解析自检通过）

## 下一步（第 1 轮）

- [x] 本机跑通 SimuroSot5.exe，验证 DLL 加载 + 队名显示（2026-08-29 起可自动化跑真实比赛）
- [x] 和官方 demo 打一场，确认 baseline 水平（真实基线 = **demo 3.5 : 我们 0.8，2 胜 1 平 11 负**，见第 10 轮勘误）
- [ ] motion 参数标定（速度/转向增益 Ka 表）
- [ ] shoot 模块实弹调优（能不能进点球——真实点球 0/3 全丢，优先！）
- [ ] 记录所有调参数值到 `docs/06-调参记录.md`

## 第 8 轮（2026-08-22）— 固定角色 + 攻防状态机

- [x] **固定角色**：role_assignment 改为固定映射（0=GK/1=ACTIVE/2=ASSIST/3=MID/4=PASSIVE），消除角色抖动→无效转向（训练赛"速度慢"根因之一）
- [x] **攻防状态机**：strategy.cpp 滞回 3 帧翻转攻防态 + state_transition 事件标志 + 威胁等级稳定分级（0.1/0.4/0.6/1.0）
- [x] **站位状态感知**：situation.cpp 进攻/防守双锚点；助攻/中场不进对方禁区（外沿 85cm）；球攻进对方禁区时回撤中线防反击（训练赛"总跑对方禁区"根因）
- [x] offline_test 新增 team state / fixed roles 用例，ALL TESTS PASSED
- [ ] 下一轮：motion 提速（drive 上限 70→90+）+ active 带球逻辑（B 模块）+ 门前回撤联动测试

## 第 9 轮（2026-08-26）— 禁区纪律接线完成（防送点球）

- [x] **对方门区禁入接线**（docs/13 方案 A+B 全量）：clamp_out_opp_goal_area 接入
  run_passive（人盯人站位 + 盯人目标过滤）/ run_assist / run_midfield / plan_defense 兜底共 4+1 处
- [x] **锚点防护带**（situation.cpp）：门前半撤触发扩到门角外球（距门线 <55cm）；passive 锚点不进对方门前 65cm
- [x] **统计口径修正**（sim_bench）：门区 y 判定 [62.5,117.5] → [75,105]，与 field_info 一致（旧宽框几何不成立）
- [x] **A/B 验证**（50 场×2 种子）：门区违规 11.1 → 0.4~0.5 次/场；我们场均 6.5 → 5.7~6.2（结构性代价，sim 未模型化点球进球，真实应净赚）
- [ ] 下一轮：真实平台验证点球次数（目标 <0.5/场）；motion 提速；B 模块 active 带球

## 第 10 轮（2026-08-29）— 真实基线勘误 + 真实平台短板实测

- [x] **⚠️ log 比分格式勘误**：官方 SimuroSot5.log 比分 = **(黄 : 蓝)**（首位黄队）。
  此前"11 胜 2 负 2 平、2.7:0.8 赢 demo、7:2 我们赢"全是误读（rlg 物理越线逐球核对推翻）。
- [x] **真实基线（勘误后）**：我们(蓝) vs demo(黄) **2 胜 1 平 11 负，场均 demo 3.5 : 我们 0.8**（14 场 log 段尾完整场次）——打不过 demo
- [x] **sim 判定降级**：sim 只能相对 A/B（改参数前后比），绝对比分与真实方向相反（sim 我们赢脚本 6:1，真实 demo 赢我们）
- [x] **真实平台短板实测**（8/29 镜像内战 rlg 全解析）：
  ① 禁区 2+ 人违规 = 0（接线生效 ✅），但 **ACTIVE 门区停留>20 周期送 3 个点球**（方案 C 未做）；
  ② **点球执行 0/3**：带球射门系统性偏下（球 y 90→65 偏出门柱），主罚助跑 175cm 太远；
  ③ **FreeBall 13 次/场**：球 75% 时间静止，右下角卡球无人救
- [ ] 下一轮（按优先级）：① 修点球/单刀射门瞄准（shoot/motion）；② ACTIVE 门区停留时限（方案 C）；
  ③ 角区救球；④ sim 脚本对手按 demo 强度重标（进攻/防守都弱于真实，需双向校准）

## 第 11 轮（2026-08-30）— 真实三大短板修复完成 + 部署待验证

- [x] **点球 0/3 根因修复**：8/29 逐帧还原 = 死球等待拦截（ACTIVE 助跑后停 (85,90) 等球，从不踢），
  不是射门瞄准。`in_penalty_exec` 标志（strategy.cpp 维护）+ run_active 死球等待排除 →
  点球走正常射门；B 队员并行已做"直线推穿"射门（贴球后球前 20cm 推穿，治系统性偏下）
- [x] **方案 C 扩展为全角色**：run_active 双计数撤出（8/18）+ strategy.cpp 5.5 兜底
  （任何非门将门区连续 >15 帧撤出到 70cm + 冷却 30 帧，冷却期调度前置拦截跳过角色函数，
  防交替目标抖振卡区）；豁免"球在门区且贴球"攻门作业与点球执行
- [x] **禁接线 clamp 调用点恢复**（并行合并时丢失）：run_passive/run_assist/run_midfield 3 处 + 方案 B 盯人过滤
- [x] **sim_bench 单人停留统计口径**：加活动度过滤（帧间位移 <2cm 才算停留，追球穿过/撤出不算）
- [x] **sim 脚本对手强度标定档**（--strength）：扫描 1.0~3.0 → 选定 **2.0**（sim 我们 2.9:5.4 净 -2.5，
  对齐真实 demo 3.5:0.8 净 -2.7 的方向与失球量级）；sim_eval 默认 strength 2.0；docs/12 §7 记录
- [x] **sim 回归**（50 场 seed 1，新档）：我们 2.9:5.4；门区违规 0.5；单人停留 3.1；offline_test 全绿
- [x] **DLL 已部署** C:\Strategy（128000B，旧版备份 backup_20260830）
- [ ] **待真实平台验证**：① 点球 0/3 → 是否命中；② ACTIVE 带球滞留是否还送点球（方案 C 撤出+冷却）；
  ③ 角区救球（FreeBall 13 次/场目标降）

## 第 12 轮（2026-08-31）— 8/31 镜像内战 0:0 复盘 + 死球处理修复

- [x] **8/31 5:36 镜像内战 0:0 复盘**（rlg 逐帧）：球 12s 后卡蓝门区 (205,90) 静止 135 秒；
  点球判罚 0 次（方案 C 生效 ✅，8/29 是 3 次）
- [x] **根因**：① 死球等待无超时（进攻方永等对方开球）；② 门将清球目标=球后 14cm 不穿过球
- [x] **修复**：死球等待 >100 帧超时主动推球；门将清球改球前 30cm 穿球推出门区
- [x] offline_test 全绿；sim 持平（sim 60 帧重置覆盖不到死球场景）；DLL 已部署 backup_20260831
- [ ] **待复跑验证**：镜像内战不再 0:0 僵死；vs demo 大比分是否改善（进攻停滞与死球处理的关联）

## 第 13 轮（2026-08-31）— 首次真 vs 官方 demo：2:7 大败 + 手动部署修复

- [x] **官方 demo 部署踩坑**：`yellow\Team2.dll`（54272B）导出 `Create/Destroy/Strategy`（老 SimuroSot-Middle
  接口），SimuroSot5.exe 报"无法定位程序点"；正确 demo = `backup_official\Strategy4Yellow.dll`（64512B，
  PDB 路径 SIM5-2018 官方编译，5 个 mangled 导出匹配）→ 已部署
- [x] **15:17 vs 官方 DEMO 战果：2:7（黄:蓝）**；我们被判 **2 次点球均未失球** ✅（点球放行修复生效）
- [x] **逐帧复盘**（rlg 8002 帧全覆盖）：
  - **进攻改善明显**：进 2 球（历史 vs demo 场均 0.8）；两球均为反击推进——
    f4419（110.5s）右路 B3/B5 接力到门前、f6806（170.2s）底线 B2 突进推射
  - **丢球 7 个中 4 个同模式**（80.1s/104.5s/127.8s/153.9s）：demo 将球控停在我方门区前
    **(205,90) 静止 58~90 帧**（1.5~2.2 秒），门将 B1 无有效清球动作，demo 随后推射进球；
    另 3 球为 demo 中前场推进直接打门（44.8s/59.5s/135.9s），门将扑救不及
  - **根因假设**：门将清球条件 `hypot(vx,vy)<2.0 && dist_our_goal<45` 要求球近静止；
    demo 带球缓慢逼近（球速 >2）时门将退守门线不主动出击抢断 → 被推射
- [x] **修复（第 13 轮后半）**：
  - ① **run_goalie 死球清球改"直线推穿"**（与 run_active 射门同款）：已贴球(≤25cm)
    → 目标=球前 20cm 穿过球推出门区；还远 → 先到球后 8cm 对准再穿。旧版直接
    position 到球前 30cm，motion 弧线绕行碰不到球（rlg 实测门将到 (165,93) 停住、
    球在 (205,90) 未动）——这是"控停门前被推射"4 球同模式的第一层根因
  - ② ~~run_passive 门前保护~~（尝试后**撤销**：球停门前时门将离球最近→球权判定
    `we_have_ball`→threat_level=0.1，分支无条件触发把 B5 拉离盯人/防区；
    sim 净胜球 -2.8→-3.5 变差，已回滚）
- [x] **验证**：offline_test 全绿；sim（50 场 seed1 strength2.0）门将推穿版
  2.7:5.8 净 -3.1（Δ-0.3 在 ±0.5 噪声内≈持平），但**过程指标全面改善**：
  射门 290→326（+35）、控球 27.0→27.6%、角区救球 73→90（+17）、争球重置 -0.7
- [x] **DLL 已部署** C:\Strategy（128512B，黄队=官方 demo 64512B 不变；
  旧版备份 backup_20260831\Strategy4Blue_pre_goalie_push.dll）
- [x] **16:27 复跑 vs 官方 demo 仍是 2:7**，但崩得更早（36.9~88.5s 连丢 5 球）：
  - 门将推穿生效（门将冲球 vl=160+），但**问题本质不是门将不够快，而是门前 1v1**：
    demo 追击手 Y5 用身体卡住球，门将冲到球前 22~25cm 被 Y5 挡着够不到球，Y5 顺势推射
  - 另 3 球为 demo 中前场推进直接打门（44.8s/59.5s/135.9s），门将扑救不及

## 第 14 轮（2026-08-31）— 防守结构性改造（借鉴官方 demo CenterDefender 球-门连线思想，自研实现）

### 读官方 demo 源码（Strategy4Yellow.cpp）提炼的防守思想
1. **CenterDefender（中卫）永远钉「球-门连线」**：球远(x>130)站中场 (110,90) 护门；
   球贴边站 (ball.x-45, 90)（球后 45cm 中路）；球进门前(x<25)站 (25, ball.y) 门前护线
2. **全队防守收缩**：球回本方半场时 4 个外场球员全部回到球-门连线之间的区域
3. **Goalie**：球过中线站门中央；球回本方按球 y 分区站位（不是死守一点）；球贴门(x<12)出击

### 我方机制缺口（2 场 2:7 逐帧复盘确认）
1. **球-门连线无人钉守**：球控停门前 (205,90) → threat=1.0 → passive 走人盯人，但盯人
   站位被 `in_penalty_area → 85cm 线` 推出；double_team_point 球进罚球区直接 return false
   → **门前只剩门将 1v1**，demo 追击手从 100cm 外直冲抢点，途中无任何我方拦截
2. **区域防守纪律过严**：plan_defense 断球点、double_team 夹抢点一落罚球区就被推出，
   防线压不上去，禁区内空虚

### 改动（借鉴 demo 思想，全部自研实现）
1. **defense.hpp/cpp 新增 `goal_cover_point()` 球-门连线护门点**：
   - 球距门 >100cm：球-门连线、球向门方向 45cm（对应 demo 中卫 ball.x-45）
   - 球距门 45~100：门前 50cm 拦截线、y 跟球（压上断球）
   - 球距门 <45：门前 20cm 线、y 跟球（堵射门角度，对应 demo 门前 25cm）
   - 不做禁区纪律（防守方进己方罚球区合法），只 clamp 场地边界
2. **roles.cpp run_passive 门前协防分支**（置于人盯人之前）：
   - 触发：球在门前 80cm 内 **且** 球近静止(<3cm/帧) **且** 对方已逼近(<60cm)
   - 站位：goal_cover_point() —— 卡死对方直冲门前的路径，与门将双人包夹
   - 与第 13 轮撤销的"门前保护"区别：**加"对方逼近<60cm"条件**——我方门球/清球
     场景（对方离球远）不触发，不拉离防区（上次 sim -3.5 的根因）
3. **defense.cpp double_team_point 解锁禁区纪律**：
   - 球进己方罚球区不再 return false：持球者已压到门前 45cm 内 → 允许第二人进禁区协防
   - 夹抢点由"落罚球区推出 85cm"改为"落门区(小禁区)推出 55cm"——罚球区内允许

### 验证
- offline_test 全绿（新增 goal cover 单测：远球连线/中近拦截/贴门堵射/斜向 clamp）
- [x] **sim 回归（50 场 seed1 strength2.0）**：defrework 2.7:4.6 净 -1.9
  （对手 5.7→4.6 -1.1，违规 0.6 持平）；100 场确认 2.8:4.8 净 -2.0
- [x] **DLL 已部署**（129024B，黄队=官方 demo 64512B 不变）

## 第 15 轮（2026-08-31）— 16:58 复跑 0:6 被零封：护门点站位修正 + 门将推穿对准

### 16:58 战果：黄 6 : 0 蓝（官方 log），开场 4.7s 就丢球，全程零封
- 丢球 6 个：**3 个仍是"控停门前 (205,90) 推射"**（82.6s/188.5s/205.3s）、
  2 个 demo 快速推进打门（4.7s 开场/215.8s）、1 个角区带出推射（179.1s）
- **进攻归零**：sim 里控球 31% 有改善，但真机被 demo 压制拿不到有效进攻

### rlg 逐帧诊断（build/_rlg_cover_diag.py）揪出护门点两个 bug
1. **`goal_cover_point` 贴门分支被 kMaxX=208 截断**：球停 (205,90) 时护门点应站
   "球与门之间"（x=213），但末尾 `clamp(out_x, kMinX, kMaxX)`（kMaxX=208，断球点
   约束）把站位压回 208——**B5 实际站在球的场侧（球后），对方推球时追不上同向的球**
   （f3304：B5 到位 (200,86) 时球已被推进 (220,89)）。已修：贴门分支单独 clamp
   到门线前 3cm（x≤217），不走 kMaxX
2. **贴门分支原本就站错侧**：旧代码站"门前 20cm 线"（x=200）也在球后。已改为
   **站「球与门之间」、球前(朝门方向)8cm**——对方追击手一推球就撞在协防者身上
   （demo 中卫站 (25,ball.y) 同理，对黄队 x=0 门，球 x<25 时 25 在球门侧）
3. **触发太晚**：oppmin<60cm 才触发，B5 从后场到门前要 40+ 帧，Y5 从 60cm 冲球
   只要 ~24 帧——永远晚到。已改 oppmin<100cm 提前出发

### 门将推穿 y 对准修复（f3304 追加发现）
- rlg：门将 B1 在 (206,82)、球 (207,90) 距 8cm<25 触发"已贴球→球前 20cm"，
  目标 (185,90)——但 B1 从 (206,82) 斜线去 (185,90) **路径不经过球心**，又绕过球
- 已修：只有 **y 与球对准(≤3cm)** 才直线推穿；未对准先到球后 8cm 对准下一帧再穿

### 门将门前抢球条件放宽（第15轮核心，读 demo Goalie 源码后修正）
- **误读 demo 修正**：demo Goalie 是 `ball.x<12 → Position(ball)` 主动压球抢断、
  **不看球速**；CenterDefender 站球后 25cm 是补防二次球，不是挡推射
- 我们的门将只在球静止(spd<2)时推穿 → **Y5 一碰球 spd>2，门将立即退回门线**，
  球被推入（f3304：门将距球 8cm 却挡不住）
- 已修：球距门<45cm 且（球静止 **或** 对方持球者离球<25cm）→ 门将保持冲球推穿，
  对方碰球期间不中途放弃（参考 demo aggressive 出击思想，自研实现）

### 验证
- offline_test 全绿（goal cover 单测更新：贴门=球前8cm x213、极贴门 clamp 到 217）
- sim 回归：
  - defrework（第一版，60cm 触发+站球后）：2.7:4.6 净 -1.9（对手 4.6）
  - defrework3（100cm 触发+站球前+门将 y 对准）：2.6:5.9 净 -3.3——控球 31%/救球 87
    改善但失球回升；sim 脚本对手不玩"控停+冲射"，这些参数在 sim 里是副作用，
    **真机场景只能真机验证**
  - defrework4（+门将门前抢球条件放宽）：待补
- **真实验证**：复跑 vs 官方 demo，看"控停门前推射"是否被 B5（球门侧）+ 门将
  （对方持球也保持冲球）双保险挡住；丢球数是否下降

## 问题日志## 问题日志

| 日期 | 问题 | 结论 |
|------|------|------|
| 08-02 | 官方 DLL 是 C++ mangled 导出名 | 必须 MSVC 32 位编译，禁止 extern "C" |
| 08-02 | 平台 i386 | MinGW(x64) 不可用，cmake -A Win32 |
| 08-02 | 官方模板 SimuroSot5.exe 首解压后消失 | 疑似杀软隔离，重解压即恢复 |

## 2026-09-02（攻击强化 + 防守修复收官，第 17-18 轮）

- **用户判定**（9/2 官方平台测试）：防守差不多，主攻需加强 → 反击快攻 A + 留反击支点 C 起步。
- **A+C**：失球转换检测（counter_attack_frames=30）+ 防守站位威胁分级（支点提前压中线）。
- **E1+E2**（用户选择）：ASSIST 双人逼抢 + PASSIVE 盯人转贴身逼抢——真机从 0 进球
  到连续 3 场运动战进球（1:1、1:2、1:1），首次攻破 demo 球门。
- **门区纪律**：追球锥形区 gate（门前 100cm×|y-90|<45）把 2+ 人违规 2.3→1.1 次/场。
- **F2 幻影球**：球速跳变滤波（30cm/帧）+ chase_target（预测离球>80cm 追实际球）——
  修 13:37 场 ACTIVE 追幻影 40 帧脱位；12 阈值教训（0:3：demo 传球 >12cm/帧 被滤成
  "静止" → 死球等待误触发、进攻瘫痪）→ 30 阈值。
- **点球 0/2 根因**：plan_shoot 70cm 射程阈值在罚球点（92cm）恒不可行 → 8/29 只修了
  门球等待拦截、漏了 plan_shoot；放宽实验致真机 0:3（A/B 确认有副作用）→ **回退**。
- **真机波动大**（同码 vel30：14:32 场 1:1 vs 15:05 场 0:1）；demo 开场控停门前套路
  连续 4 场 3.2s 丢球（GK 冲球震荡未解，留待真机物理级调试）。
- **最终收敛 = vel30 版**（用户拍板）：sim 3.8:4.7（基线 2.6:5.9，净胜球 -3.3→-0.9），
  射门 300→419，门区 2+人 1.1 次/场；真机 1:1 级别、稳定自主进球。已部署。

## 2026-09-09（P0 轮：进攻强化 + 图论路径 + 机会质量射门，docs/15 方案）

- **spec 先行**：docs/15-激进进攻与路径规划方案.md（P0/P1 两期设计，用户已确认 4 项决策）。
- **P0 交付**（全部落地 + offline_test 18 用例全绿 + sim A/B）：
  1. geometry 圆盘碰撞原语 → 射门/传球/路径判定口径统一；
  2. route 可见图 + Dijkstra 避障路径层（含圆-圆外公切线、拉直、2r 邻域裁剪，
     追球已接线避障；move_avoiding 接口就绪）；
  3. plan_shoot 机会质量：净开口角（GK 遮挡几何）+ 连续 aim + 70~110 动态射程
     「开口≥8°+路线通」双闸门（≤70cm 维持旧无条件射——sim A/B 关键校准）；
  4. run_active：动态区 quality≥0.35 远射档 + 追球避障。
- **sim A/B**（50 场×seed1/2）：基线净胜 5.8/5.4 → P0 5.5/5.1（-0.3，噪音内；
  成本 = 弱脚本 GK 前闸门拒掉的边际盲射，真实 GK 强时方向相反）。射门次数降但
  单射效率升（场均进球持平）。详细隔离记录见 docs/06 第 37 轮。
- **教训**：①"激进"不可靠一刀切（70→110 直放真机 0:3 前科）→ 机会质量闸门 +
  sim A/B 隔离逐项验证；②中档带球逼近分支 sim 净胜 6.9→4.4 → 移除，带球还原基线；
  ③全圆路径图每帧 0.3ms 太贵 → 邻域裁剪（只规划影响 S→T 的圆）。
- **待办**：P1（自数比分越线锁存 + 领先 ≥2 转防守加强：threat 上调/锚点后撤/
  反击窗口缩短/冒险度降）；无球跑位避障接线（可选）；真机部署验证远射档。

## 2026-09-09（P1 轮：自数比分 + 领先 ≥2 转防守加强，docs/15 §3）

- **自数比分**：WorldModel score_us/score_them/lead_by_two/ball_seen_moving +
  strategy::update_score。判据 A = PlayOn→PlaceKick 事件（进球后失球方开球，
  docs/06 L347 实测）；判据 B = 越线兜底。**实测关键教训**：越线状态对策略不可见
  （进球后平台立即重置）→ 越线判据在真实/sim 都不可达，事件判据是唯一通路。
- **sim_bench 升级**：进球后模拟失球方开球（PlaceKick 60 帧）→ 自数端到端验证
  （冒烟逐场一致 4:7/8:1/4:6）；A/B 均带此模拟。
- **防守链**（lead_by_two ≥2 触发）：threat +0.2（提早人盯人）、锚点后撤 20cm、
  反击窗口 30→15、射门立即档 0.35→0.55、围困半径 25→22/贴身 12→15。
- **sim A/B**（带 pm，50 场×2 seed）：防守链净效果 seed1 3.3:4.0→4.1:4.0
  （+0.8）、seed2 4.3:3.5→4.7:3.9（持平）——不回退且 seed1 改善。
- **offline_test 24 组全绿**（新增 score counting 12 断言 + lead defense 决策分离）。
- **P0+P1 全部交付**。真机待办：①远射档/动态射程实机验证 ②PlaceKick 判据半场
  风险验证（若中场 PlayMode 保持 PlayOn → 下半场黄开球误计，需加半场守卫）
  ③带球绕人 route 仍默认关（spec §6.3 风险 3）。部署 DLL 未做（待真机前统一）。
- **代码清理（僵尸函数移除，零行为影响）**：静态排查 src/include 全仓调用关系，
  删除 3 组无调用方代码：`motion::angle()`（motion.hpp/cpp，原地转向，已由
  position/chase_ball 内联姿态控制取代）、`field_info::in_field()`（场地内判定，
  无人用）、`WorldModel::ball_opp_goal_dist()/ball_our_goal_dist()`（inline 距离
  助手，无人用）。构建 + offline_test 全绿 + sim 10 场冒烟（4.9:3.9）无回归。
  ⚠️ 职责提醒：B 模块 motion.cpp 顶部基座注释仍提官方 demo Angle()，仅剩
  Position() 系算法，注释已同步。
- **部署官方平台（2026-09-09）**：构建 Strategy4Blue.dll（含 P0+P1 全链 + 僵尸清理，
  SHA1 AAE3580CF6BB，158720B）→ 备份 `C:\Strategy\backup_20260909\`（旧 Blue
  pre_P1 + 官方 Yellow）→ 覆盖 `C:\Strategy\Strategy4Blue.dll`。黄队侧保持官方
  demo 不动。平台 SimuroSot5.exe 加载根目录 DLL。
  真机验证清单（对应 docs/15）：① PlaceKick 判据半场风险（中场 PlayMode 若保持
  PlayOn → 下半场黄开球误计，观察比分数即可发现）；② 动态远射档（70-110cm
  开口闸门）实战表现；③ 带球绕人 route 默认关，追球避障观察。

- **2026-09-09 禁区围堵轮（docs/16）**：官方冲禁区推球问题 → 根因=防守动态目标
  被钳出大禁区(x=135)留无人走廊 → 改为小禁区口径(x≈165)大禁区放行 + double_team
  提前(145cm)合围。offline 25 组全绿；sim A/B 净胜噪音内持平、控球 +0.3~0.8；
  sim_bench 加"己方门区非门将"监控统计（甄别为 goal_cover 贴门战术非违规）。
  真机验证待办：demo 推进是否被挡在禁区外。

## 2026-09-09（回退轮：真机效果差 → 回二修版基线 5eaa5b1）

- 禁区围堵(08a9d04) 真机效果差，用户指令回退到 git 历史成绩最好的一版。
- 甄别结论：**5eaa5b1（二修版基线）** = 真机/sim 成绩最好版本
  （sim 6.9:1.1/6.2:0.8 全历史最高；三个实验批次 revert 均回归它）。
- 执行：代码恢复 5eaa5b1 全树（含 offline_test/roles/shoot/strategy/defense/
  sim_bench 等 19 文件）；删除 docs/15、docs/16、route.hpp/cpp；docs/03/06
  保留全部历史记录。
- 验证：offline_test 全绿、sim 与基线逐位一致、DLL 已部署官方平台
  （backup_20260909 有围堵版备份）。
- 教训：P0+P1/围堵两轮 sim A/B 数字不差（±0.3 噪音内），但真机口径差——
  sim 的 scripted 对手 ≠ 官方 demo（5 人墙战术/冲禁区推球），下一步迭代
  应优先做 demo 战术对手模拟 + 小步真机验证，别信 sim 持平就上真机。

## 2026-09-10（第 44 轮：运动控制制动包线 —— docs/18 P1，真机标定驱动）

背景：用户判定"有效射门非常少"，指示**先优化 motion**（射门函数下一轮）。
射门链依赖"精确到位 + 正对推球方向"，而 motion 既不保证停住也不保证朝向。

- **真机标定工具**（新增 `tools/py/motion_calib.py`，输入 `build/traj_before.csv`
  2026-09-10 真机轨迹 5559 帧 ≈139s）+ 三条与旧结论相反的发现：
  1. **我们不慢**：稳态速度 p50/p90 = 79.8/115.3 vs demo 67.9/97.9
     （`motion.cpp:27` 旧注释"我们 55-88"作废）；
  2. **静止帧占比 13.4% vs demo 3.5%（3.8 倍）**、**低速来回蹭 2.3 vs 0.9 次/机器人/分钟
     （2.6 倍）** —— 瓶颈是"停不住/稳定不下来"，不是速度上限；
  3. 标定常数：减速 p95 = 658 cm/s²（同口径噪声底噪 18 → 信号/底噪 17，可信），
     1 轮速单位 ≈ 1.0 cm/s（命令饱和 112.5 ↔ 实测 p90 115.3）。
- **算术根因**：从 115cm/s 停下需 10.0cm（`v²/2a`），旧律却在 `de<1cm` 才命令停车
  → 过冲必然 → `te` 翻 180° → 落进倒车分支 → 极限环。这正是上面 2.6×/3.8× 的机制。
- **改动**（docs/18 P1）：`motion.hpp/cpp` 新增 `TargetMode{TM_STOP,TM_PASS}`
  + 制动包线 `v ≤ √(2·kBrakeAccel·(de−kStopEps))`（`kBrakeAccel=400`，
  双积分器时间最优的切换曲线）+ 输出安全夹取 + NaN/Inf 目标停车；
  删除与包线冲突的旧减速条款；`roles.cpp` 按语义标注调用点
  （射门推穿/救球/带离/带球推进/E1 压上/门将推穿 = TM_PASS，站位点 = TM_STOP）。
- **验证**：
  · `offline_test` 26 组全绿（新增 4 组）：真机口径物理积分**过冲 0.0cm**（旧律 9.2cm，
    理论 10.0cm）；sim 口径残余 2.7cm（理论 3cm，已锁界 ≤5cm）；
  · sim A/B（50 场×**4 种子**，基线由 HEAD 版本单独编译）：净胜球均值 **+0.05、
    标准差 0.66 → 在噪声内，不能声称胜负改善**（⚠️ 只跑 seed1/2 时曾误判
    "+0.6 超噪声带"，补 seed3/4 后推翻——教训见 docs/06 第 44 轮方法学结论：
    单种子 50 场要 >~1.3 球（2σ）才算证据，"±0.5 噪声带"低估了噪声）；
  · 可信证据 = 过程指标 **4/4 种子一致改善**：单人门区停留 31.1→23.0 帧/场（-26%）、
    角区救球 104.5→92.9 次/场、争球重置 108.5→107.3；反向项：控球 -0.65pp、
    射门代理 -12 次/场（待真机判断）；
  · **关键先例**：docs/06 第 18 轮试过"motion 全局减速"→ sim 崩（得分 3.8→2.3、
    射门 419→195）被否决；本轮 `TM_STOP/TM_PASS` 语义切分正是防这个失败模式
    （射门仅 -10%，未腰斩）；
  · `sim_bench` 新增 `--traj out.csv`（列名与真机 rlg 导出一致）→ 同一把尺子量 sim/真机：
    速度 p50 40.4→44.1、蹭频 1.0→0.8、静止带内 路程/净位移 1.58→1.34（抖动更少）。
- **待办**：① P2 `position_aligned`（到点定向，射门前置）；② 射门函数优化
  （`plan_shoot` 70cm 射程闸门在罚球点 92cm 恒不可行；两段式靠 `|te_ball|<40°` 凑朝向）；
  ③ **真机验证**（跑一场 → `motion_calib.py` 复量 `traj_after.csv`：
  静止占比 13.4%↓、蹭频 2.3↓）；④ 32 位 MSVC DLL 需在 VS 提示符下
  `cmake -A Win32` 出包（本机无 MSVC，验证用 MinGW g++ 编 64 位
  `offline_test`/`sim_bench`）。
- **注意**：sim 数字好看 ≠ 真机不差（09-09 回退轮教训），本轮尚未真机验证。

## 2026-09-11（第 45 轮：官方 demo 黄队「禁区纪律」——治判罚打断，便于观察蓝队算法）

用户问题：真机 vs 官方 demo 的比赛被平台判罚反复打断，"无法直观观察算法优化效果"。
用户选定：改**官方 demo（黄队）源码**这一侧。

- **取证**（新增 `tools/py/referee_diag.py`）：`.rlg` 只录 PlayOn 帧（gs/whos 恒 0），
  所以判罚改用「摆位瞬移」识别（球或机器人跳变 >40cm）。9/9 三场 DEMO Yellow vs MyTeam Blue：
  停表 **9.5 / 20.2 / 13.2 次/分钟**；其中 **73~79% 的停表前黄队贴到蓝队门将 <12cm**、
  68~81% 是黄队有人在我方门区、55~77% 是门区 ≥2 人。
  典型死循环：19:32 场末段球停 (205,90)，黄队 3 人挤进小禁区距门将 8~9cm → 判门球给蓝
  → 摆位 → 再冲进来，**1.4s 一轮、最后 17 秒 14 次**。根因＝demo 四个外场球员全员追球冲小禁区。
- **关键发现**：`C:\Strategy\Strategy4Yellow.dll` 已是 `C:\Strategy\.tmp\demo_yellow`
  的可编译**官方 demo 源码**（SHA1 一致）→ 黄队可改；而现役 `Strategy4Blue.dll`（148480B）
  ＝第 41 轮路径优化版，**不含第 42/43 轮（补射/变角推射）**，也不含第 44 轮 motion（未提交）。
- **改动**：`tools/py/patch_demo_yellow.py` 给 demo 加「四道闸」（目标红线 164 / 大禁区最多 1 人 /
  实际位置 >168 退回 / 门将保护圈 20cm / 蓝队死球退回 130），替代 9/9 只 clamp 站位目标的版本。
- **验证**：demo 工程单测 `test_discipline.exe` **10/10 通过**；导出符号与官方 demo 逐字相同
  （5 个 mangled 名，`tools/py/pe_exports.py`）；Win32 i386；DLL 已部署（14848B，SHA1 F08C744D…），
  旧版备份 `backup_20260911`。
- **待验证**：真机跑一场看停表次数是否 9.5~20.2 → <3 次/分钟，且 demo 仍能正常施压。
- **遗留**：我方蓝队侧「非门将进己方门区」7~12 段/场（最长 123 帧）是另一个中断源，本轮未动。
- **后续（9/11 晚）**：v1 的 ④门将保护圈、⑤死球全队退回已按用户指令删除，补丁重写为 **v2**
  （只留 ①②③ + 修门球落点 (10,70)→(10,90) 死锁），已部署 15360B / SHA1 6DF4E42E…
  （见 docs/06 第 48 轮）→ **本轮的 v1 源码副本 `build\demo_yellow_patch` 不要再出包**。
  另：该 demo 工程默认 **/MD**（官方 demo 与我们蓝队 DLL 都是 /MT），本机装了 VC++ x86
  运行库所以能跑（09:44 场实测正常，**不是**卡死原因），但换机器可能 LoadLibrary 失败
  → 出 /MT 包用 `tools/ps1/fix_demo_yellow.ps1 -Build`（默认只检查，-Deploy 才覆盖）。
- **本轮产出工具（仍在用）**：`tools/py/referee_diag.py`（判罚归因量尺：摆位瞬移识别停表 +
  停表前犯规特征）、`run_match.py` / `stop_match.py`（自动化跑验证赛；**平台窗口必须置顶可见**，
  被盖住时时钟停在 300 不出 .rlg）、`pe_exports.py` / `pe_imports.py`（导出符号 / 运行库依赖校验）。

## 2026-09-11（第 46 轮：route.cpp 五项顺手优化 + 属性测试抓到一个真 bug）

用户点名 review 时列的 5 条 route 优化，逐条处理（详见 docs/06 第 46 轮、docs/18 §7）：

- ① 删 `plan_route_sub` 里重复的直线检查（**死代码**，子集被挡可证）；② `add_vertex`
  **保持算法不动**（V≤62，O(V²) ≈1900 次比较可忽略；"只对本批次查重"会破坏正确性），
  改容量守卫前置 + 注释说明；③ 3r 补算 → **违例驱动补算**（只补真被撞且不在图里的圆）；
  ④ `test_route_avoid` 长度界 (100,200] → **(100,110)** + margin 断言；
  ⑤ margin/inflate 契约写进 `route.hpp` + `roles.cpp` 调用点（防调小 inflate 而真撞）。
- **④ 的产物：新增属性测试 `test_route_global_safety` 抓到 HEAD 就存在的真 bug**：
  trial 128 中一个被 2r 邻域裁掉的圆（r=8.98，距线段 20.03）被绕行路径擦到，
  **距本体外缘仅 0.63cm**；机制是切线可见图表达不了"沿圆弧绕行"（弦必穿圆）→ 图上无解 →
  旧代码把会撞的路径当成功返回（found=true）。**修法：最终全圆校验，不安全就 found=false**
  （契约：found=true ⟹ 真安全）。残留限制（需弧边/Theta* 根治）已记 TODO。
- **验证**：`offline_test` **27 组全绿**；随机 200 组穿盘 **0**（修前 1）；
  最坏配置规划 **16.3µs/次**（g++ -O2 x64）；sim A/B 与 motion P1 版 4 种子比对
  **有差异但都在噪声内**（净胜 Δ 均值 -0.05）——差异来自上述契约修正，
  ⚠️ 我原以为 route 改动会逐位一致，是比对才发现预测错了。
- **待办**：① motion（第 44 轮）+ route（本轮）**均未真机验证、未出 DLL**；
  ② `sim_bench` 的 kSpeed=0.9 与真机标定 1.0 不符，改动会动全部历史 A/B 基线，需专门评估；
  ③ 弧边/Theta*（可选）。

## 2026-09-11/12（第 50 轮：黄队禁区纪律真机验证 —— 判罚 8.6~19.0 → 1.1~6.1 次/分钟）

- 新增 `tools/py/verify_demo_discipline.py`：一条命令出「改前/改后」表
  （rlg 摆位瞬移识别停表 + 官方 log 分段按时间戳匹配 + 逐条按"蓝侧/黄侧"特征归因）。
  改后样本 = 9/11 11:07 / 11:29 / 11:52 三场（黄队 v2 部署后）。
- **结果**：停表前"黄队贴蓝门将 <12cm" **73~79% → 0~13%**、"黄队在我方门区" 68~81% → 0~23%、
  "门区≥2 人" 55~77% → 0~11%；**Goal Kick Blue 15/38/34 → 0/2/0**（第 45 轮定位的
  "冲门区→判门球给蓝→摆位→再冲"死循环止住）；判罚类事件 **8.6~19.0 → 1.1~6.1 次/分钟**。
- **剩余头号来源变成我们自己**：11:29 场 `Penalty Kick Yellow` 11 次里 **8 次带"蓝侧"特征**
  ＝我方非门将赖在己方门区（守门员须独守门区）；该场 22 段 / 1755 帧（11:52 场单段 892 帧）。
  → 下一轮：把 roles/defense 的门区纪律改成以**实际位置**为准的强制退出（方案 C 是目标 clamp
  + 帧计数，管不住过冲，且护门/清球/补射豁免会叠加）。
  **用户指令（9/11）：这块交并发会话实施** → 派工单见 `docs/20-蓝队门区纪律待办（交并发会话）.md`
  （含问题数据、机制猜测、验收口径与量测命令）。
- **第 49 轮（角区不推球）已代为编译 + 跑测**：`offline_test` **31 组全绿**（含新增
  `no push zone`、`active corner rescue` 四段）；现役蓝队 DLL 与当前工作树构建产物逐字节一致
  （SHA1 `D80176AA…`, 162304 B, /MT）→ 第 49 轮**已在真机生效**。
  log 角区犯规 `blue team Violated No pushing`：10:56 蓝=4（改前）→ 11:07/11:29/11:52 蓝=0。
- 工具修复：`run_match.py` 不再按按钮文字判成败（会二次 Start 让平台退出且不落 rlg），
  改为轮询时钟；`verify_demo_discipline.py` 补上 `Violated No pushing` 行解析。
- **用户指令（9/11）**：真机比赛**由用户手动开**，agent 只做日志分析（不再自动启平台）。
- 平衡风险：11:52 场 57s 内蓝队 6:0 且黄队在我方门区 0 帧 → demo 可能被压得过于被动，
  必要时把"大禁区最多 1 人"放宽到 2 人。
